# 本機開發（先開 server：cd server && node index.mjs）
VITE_WS_URL=ws://localhost:3001

# Netlify 伺服器環境變數（不可使用 VITE_ 前綴，避免洩漏到瀏覽器）
OPENAI_API_KEY=

# 部署後改成你的 Render WebSocket URL：
# VITE_WS_URL=wss://chanceboard-server.onrender.com
